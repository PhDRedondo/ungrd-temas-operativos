# CLAUDE.md

@AGENTS.md

Antes de cambios de arquitectura o núcleo, leer `docs/MEMORY.md` y `docs/ARCHITECTURE.md`.
Para temas: solo `src/themes/<slug>/` salvo instrucción explícita.
Verificación: `npm run harness` · `npm run smoke`.
